apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: diploma-app
  labels:
    app: kafka
spec:
  type: ClusterIP
  ports:
    - port: 29092
      targetPort: 29092
      name: internal
  selector:
    app: kafka